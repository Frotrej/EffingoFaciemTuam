<Window x:Class="EffingoFaciemTuam.Windows.SequenceManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EffingoFaciemTuam.Windows"
        xmlns:models="clr-namespace:EffingoFaciemTuam.Model"
        mc:Ignorable="d"
        Title="SequenceManagement" Height="450" Width="498">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.70*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>


        <DataGrid x:Name="SequenceRepresentation" Grid.Row="0" HorizontalAlignment="Center" MinWidth="380" ItemsSource="{Binding Sequence, Source={x:Static models:SequenceStore.ShareSequence}}" AutoGenerateColumns="False" CanUserSortColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" AlternatingRowBackground="LightGray">
            <!--dodac filtrowanie automatyczne po kolumnie "numer korku"-->

            <DataGrid.Columns>
                <DataGridTextColumn Header="Krok" Binding="{Binding StepNumber}" IsReadOnly="true"/>
                <DataGridTextColumn Header="Opóźnienie" Binding="{Binding Delay}" IsReadOnly="False"/>
                <DataGridTextColumn Header="Akcja" Binding="{Binding Type}" IsReadOnly="true"/>
                <DataGridTextColumn Header="Skrót" Binding="{Binding KeyboardKeysString}" IsReadOnly="true"/>
                <DataGridTextColumn Header="MyszX" Binding="{Binding MouseX}" IsReadOnly="true"/>
                <DataGridTextColumn Header="MyszY" Binding="{Binding MouseY}" IsReadOnly="true"/>
            </DataGrid.Columns>
        </DataGrid>

        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Button Content="Testuj sekwencje" Width="100" Height="40" Margin="20,0,20,0" Click="Button_Click_TestSequence"/>

            <Button Content="+" Width="40" Height="40" Margin="5,0,5,0" Click="Button_Click_AddElement"/>
            <Button Content="-" Width="40" Height="40" Margin="5,0,5,0" Click="Button_Click_RemoveElement"/>
        </WrapPanel>

        <WrapPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10">
            <Button Content="Zamknij" Click="CloseWindow" Width="50" Height="20"/>
        </WrapPanel>
        
    </Grid>
</Window>
